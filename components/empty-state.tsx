/**
 * Moved to `components/ui/empty-state.tsx`, where the rest of the design
 * system lives. Re-exported here so the screens that already import it from
 * this path keep working; new code should import from `@/components/ui/empty-state`.
 */
export { EmptyState } from "@/components/ui/empty-state";
